# Kubernetes Starter Manifests

This folder contains initial manifests for:

- `namespace.yaml`
- `api-gateway.yaml`
- `auth-service.yaml`

For other services, duplicate `auth-service.yaml` and update:

- Deployment + Service names
- Image names
- Container and service ports
